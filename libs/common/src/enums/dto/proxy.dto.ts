export enum ProxyType {
  SOCKS5 = 'socks5',
  HTTP = 'http',
  HTTPS = 'https',
}

export enum ProxyStatus {
  Connected = 'Connected',
  NotConnected = 'NotConnected',
  Unknown = 'Unknown',
}

export enum ProxyCountry {
  Germany = 'Germany',
  Poland = 'Poland',
  USA = 'USA',
}
